---
description: 商业保密性与软件保护规范
alwaysApply: true
---

# 商业保密性规范

本项目为商用软件，所有代码变更必须遵守以下安全约束。

## 软件授权（License）

- 应用启动时必须先在 Rust 端校验许可证，校验不通过禁止进入主界面
- 许可证基于机器指纹（CPU + 磁盘 + 主板哈希）绑定，防止安装包拷贝即用
- 签名算法使用 Ed25519，公钥内嵌于 Rust 二进制，私钥由发行商离线保管
- 授权校验逻辑只允许在 Rust 端实现，前端仅调用 Tauri command 获取结果
- 禁止在前端代码中包含任何密钥、签名逻辑或可绕过授权的条件分支

## 数据库加密

- 生产构建必须使用 SQLCipher 加密数据库（`tauri-plugin-sql` 的 `sqlcipher` feature）
- 数据库加密密钥禁止硬编码在前端代码中，必须由 Rust 端派生或管理
- 备份文件必须是加密状态的副本，禁止导出明文 `.db` 文件
- 新增迁移脚本时需在加密数据库环境下测试通过

## 密码安全

- 启动密码必须使用 Argon2id 哈希后存储，禁止明文存储
- 哈希和验证操作只在 Rust 端执行（通过 Tauri command），前端不做字符串比较
- 禁止将明文密码写入日志、toast 消息或任何可观测的输出

## 前端代码保护

- 生产构建必须开启 JS 混淆（terser，`mangle: { toplevel: true }`）
- 生产构建禁止生成 Source Map（`sourcemap: false`）
- 生产构建禁止保留 `console.log` / `console.debug`（`drop_console: true`）
- 必须配置 CSP（Content Security Policy），禁止加载外部脚本
- 生产构建必须禁用 WebView DevTools

## 安装包与分发

- 发布用安装包（NSIS / MSI）必须使用代码签名证书签名
- 禁止在公开仓库、公开 CDN 或未授权渠道分发安装包
- `.exe` / `.msi` 文件名不得泄露内部版本号以外的技术细节

## 敏感信息管理

- 私钥、证书、密码等敏感文件必须加入 `.gitignore`，禁止提交到版本库
- 常见需排除的文件：`*.pem`、`*.key`、`*.p12`、`*.pfx`、`license-private.*`、`.env`
- Tauri 签名配置中的证书指纹通过环境变量注入，不硬编码在 `tauri.conf.json`

## 禁止

- 禁止在前端存储或传递明文密码、密钥、许可证私钥
- 禁止用 `eval()`、`new Function()` 或动态脚本加载
- 禁止将安全校验逻辑放在前端（可被 DevTools 绕过）
- 禁止在日志或错误消息中输出用户密码、机器指纹全文、许可证密钥
- 禁止移除或弱化已有的安全措施，除非有替代方案并经确认
