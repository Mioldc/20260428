---
description: shadcn/ui 使用约定、布局规范和交互模式
globs: "**/*.tsx"
alwaysApply: false
---

# UI 规范

## 设计原则

- 浅色主题，正文 16px，充足留白
- 所有界面文案使用中文，零英文术语
- 操作按钮突出醒目，关键操作用主色

## 窗口布局

- 左侧导航栏固定 220px，含图标 + 中文标签，当前页面高亮
- 右侧内容区：顶部面包屑 + 操作按钮区，下方页面主体
- 默认窗口 1280×800，最小 1024×700

## shadcn/ui 组件选用

| 场景 | 组件 |
|------|------|
| 数据表单 | `Form` + `FormField` + `FormItem` |
| 数据列表 | `Table` |
| 状态标签 | `Badge`（不同状态不同颜色） |
| 确认弹窗 | `AlertDialog` |
| 轻提示 | `Toast` / `Sonner` |
| 下拉选择 | `Select` 或 `Combobox` |

## 交互规则

- 核心操作 ≤ 3 步点击完成
- 表单支持 Tab 键快速切换字段
- 列表页必须支持搜索 + 状态筛选
- 删除操作必须二次确认（AlertDialog）
- 保存成功后显示 Toast 提示
- 常用操作提供键盘快捷键（如 Ctrl+N 新建）
